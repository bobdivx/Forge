---
export const prerender = false;
// Le fragment # n’est pas fiable dans l’en-tête HTTP Location : on passe un query param
// et le script en bas de /agents fait défiler vers #swarm-flux.
return Astro.redirect('/agents?chronologie=1', 302);
---

